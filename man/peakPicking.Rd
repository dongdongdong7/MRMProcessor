% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peakPicking.R
\name{peakPicking}
\alias{peakPicking}
\title{peakPicking}
\usage{
peakPicking(
  int,
  rt,
  noise = NA,
  noiseMag = 2,
  smoothPara = get_smoothPara(),
  baselinePara = get_baselinePara(),
  peakPara = get_peakPara()
)
}
\arguments{
\item{int}{MRM window intensity vector.}

\item{rt}{MRM window retention time vector.}

\item{noise}{noise, NA or set by user. If NA, noiseEs function will be used.}

\item{noiseMag}{noiseMag.}

\item{smoothPara}{smoothPara.}

\item{baselinePara}{baselinePara.}

\item{sn}{sn threshold.}

\item{preNum}{preNum.}

\item{tol_m}{tol_m.}

\item{snthresh}{This is the parameter used in the matchfilter function to determine the multi-peak case.}

\item{fwhm}{This is the parameter used in the matchfilter function to determine the multi-peak case.
The smaller the value the easier it is to find multiple peaks.}

\item{cal_ZOI_baseline}{Whether or not to calculate a baseline for the ZOI separately, which helps the accuracy of edge finding.}

\item{multiSmooth}{Whether or not to give multiple smoothing to the ZOI region, which helps the accuracy of edge finding}

\item{extend}{extend.}
}
\value{
A list.
}
\description{
This is a peak-picking function for the MRM targeting window
}
\examples{
row <- 200;col <- 2
plot(x=data[row,col]@rtime * 60,y = data[row,col]@intensity, type = "l")
plot(x=data[row,col]@rtime * 60,y = smoothFun(data[row,col]@intensity), type = "l")
baseline <- baselineEs(int = smoothFun(data[row,col]@intensity), rt = data[row,col]@rtime * 60)
lines(x = data[row,col]@rtime * 60, y = baseline)
noise0 <- noiseEs(smoothFun(data[row,col]@intensity))
noiseEs(data[row,col]@intensity, prepare = FALSE)
abline(h = noise0)
plot(sort(data[row,col]@intensity))
peakPicking(int = data[row,col]@intensity, rt = data[row,col]@rtime * 60, noise = 1000, peakPara = get_peakPara())
}
